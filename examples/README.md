# AllEnricher v2 Examples

This directory contains a tiny, deterministic figure gallery used by the main README.
It is not a replacement for real database-backed analysis. Its job is to show what
AllEnricher result tables and publication figures look like.

Run from the repository root:

```bash
python examples/run_examples.py
```

Generated figures are written to `examples/output/figures/`.
